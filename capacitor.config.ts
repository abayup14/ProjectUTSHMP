import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project_uts_hmp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
